{% extends 'base.html.twig' %}

{% block title %}Telefone{% endblock %}

{% block body %}
    <h1>Telefone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telefone.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ telefone.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_telefone_index') }}">back to list</a>

    <a href="{{ path('app_telefone_edit', {'id': telefone.id}) }}">edit</a>

    {{ include('telefone/_delete_form.html.twig') }}
{% endblock %}
