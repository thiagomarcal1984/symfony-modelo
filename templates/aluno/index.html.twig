{% extends 'base.html.twig' %}

{% block title %}Aluno index{% endblock %}

{% block body %}
    <h1>Aluno index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for aluno in alunos %}
            <tr>
                <td>{{ aluno.id }}</td>
                <td>{{ aluno.nome }}</td>
                <td>
                    <a href="{{ path('app_aluno_show', {'id': aluno.id}) }}">show</a>
                    <a href="{{ path('app_aluno_edit', {'id': aluno.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_aluno_new') }}">Create new</a>
{% endblock %}
