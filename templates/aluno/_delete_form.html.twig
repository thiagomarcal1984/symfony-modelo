<form method="post" action="{{ path('app_aluno_delete', {'id': aluno.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aluno.id) }}">
    <button class="btn">Delete</button>
</form>
